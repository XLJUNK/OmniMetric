name: Global Macro Signal Update
on:
  push:
    branches: [ main ]
  schedule:
    - cron: '5,20,35,50 * * * *' # Every 15 minutes, offset by 5m (captures :00 indicator results)
  workflow_dispatch:

concurrency:
  group: update-signal
  cancel-in-progress: false

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
          cache: 'pip'
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests yfinance google-generativeai fredapi pandas numpy lxml python-dotenv
      - name: Commit and push changes
        env:
          FRED_API_KEY: ${{ secrets.FRED_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          FMP_API_KEY: ${{ secrets.FMP_API_KEY }}
          VERCEL_AI_GATEWAY_SLUG: ${{ secrets.VERCEL_AI_GATEWAY_SLUG }}
          AI_GATEWAY_API_KEY: ${{ secrets.AI_GATEWAY_API_KEY }}
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          # 1. Sync with remote HEAD to ensure we are working on the latest branch
          git fetch origin main
          git reset --hard origin/main
          # 2. Run engine on LATEST state
          python backend/gms_engine.py
          # 3. Commit the fresh data and diagnostic logs
          git add backend/*.json backend/*.txt
          git commit -m "Auto-update market signal [bot]" || echo "No changes to commit"
          # 4. Push
          git push origin main
