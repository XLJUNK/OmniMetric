name: OmniMetric SNS Bot

on:
  schedule:
    # Hourly Check for "Monitoring Mode" (Alerts) + Market Opens/Closes covered naturally
    - cron: '0 * * * *'
  workflow_run:
    workflows: ["Global Macro Signal Update"]
    types:
      - completed
  workflow_dispatch: # Manual trigger

permissions:
  contents: read

jobs:
  post-updates:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout Repo
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10'

    - name: Install Dependencies
      run: |
        pip install -r backend/requirements.txt

    - name: Run SEO Monitor (Update Keywords)
      # Run this occasionally to freshen keywords. 
      # Since we don't have GSC keys in secrets yet, this might fail if not handled gracefully.
      # The script has a dry-run fallback.
      continue-on-error: true
      run: |
        python backend/seo_monitor.py

    - name: Run SNS Publisher
      env:
        TWITTER_API_KEY: ${{ secrets.TWITTER_API_KEY }}
        TWITTER_API_SECRET: ${{ secrets.TWITTER_API_SECRET }}
        TWITTER_ACCESS_TOKEN: ${{ secrets.TWITTER_ACCESS_TOKEN }}
        TWITTER_ACCESS_SECRET: ${{ secrets.TWITTER_ACCESS_SECRET }}
        BLUESKY_HANDLE: ${{ secrets.BLUESKY_HANDLE }}
        BLUESKY_PASSWORD: ${{ secrets.BLUESKY_PASSWORD }}
      run: |
        python backend/sns_publisher.py
